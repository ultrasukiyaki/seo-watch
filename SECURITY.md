# Security Policy

日時はDBへUTCで保存し、MySQLセッションも`+00:00`へ固定します。タイムゾーン設定変更はCSRF保護されたスーパーユーザー操作として監査し、既存日時の一括補正は行いません。

## Supported versions

原則として最新リリースのみをサポートします。セキュリティ修正が公開された場合は、できるだけ早く最新版へ更新してください。

| Version | Supported |
|---|---|
| 0.11.1 | Yes |
| 0.11.0 | Upgrade recommended |
| 0.10.2 | Yes |
| 0.10.1 and older | Upgrade recommended |

## Reporting a vulnerability

脆弱性の可能性がある内容は、公開Issueへ詳細を書かないでください。

リポジトリ所有者へ非公開で連絡できる手段を用意し、次を添えて報告してください。

- 影響を受けるバージョン
- 再現手順
- 想定される影響
- 検証に使用した環境
- 可能であれば修正案

GitHub Security Advisoriesを有効化している場合は、リポジトリの **Security → Report a vulnerability** を利用してください。

## Secret handling

次は公開リポジトリへコミットしないでください。

- `config/local.php`
- Google OAuthクライアントシークレット
- OAuthアクセストークン・更新トークン
- DBパスワード
- 本番DBダンプ
- Cronログやエラーログ
- パスワード再設定・招待・メール確認の生トークンと完全URL
- 認証監査ログを含む本番DBダンプ

漏えいが疑われる場合は、Google OAuthクライアントシークレットをローテーションし、DB認証情報とスーパーユーザーパスワードも変更してください。

パスワードや認証URLの漏えいが疑われる場合は、対象アカウントのパスワードを変更し、ユーザー管理から全セッションを無効化してください。メールが使えない場合は対話TTYで`php bin/reset-password.php --user=admin`を実行します。

## v0.10系のセキュリティ境界

- 改善タスクの更新はスーパーユーザー限定で、viewerは一覧と履歴の読み取りだけが可能です。
- タスク取得・更新はSearch ConsoleプロパティIDで制限し、別プロパティのIDを受け付けません。
- 改善履歴は追記専用で、Web画面から削除・改ざんできません。
- 同期leaseの所有者トークンはSHA-256ハッシュだけをDBへ保存し、画面やログへ表示しません。
- 同期エラーは分類済みの安全な要約だけを表示し、Googleレスポンス全文やスタックトレースを保存しません。
- maintenance CLIはWebから実行できず、分析データ、改善タスク、ユーザー、OAuth、設定を削除しません。
